import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Progress } from "@/components/ui/progress";
import { Accessibility, ArrowRight, BedDouble, Bot, BrainCircuit, ChefHat, CircleCheck, Droplets, Ear, HeartHandshake, Leaf, Mic, MicOff, Recycle, ShieldCheck, Sparkles, Sprout, Trash2, Utensils, Waves } from "lucide-react";
import { useRef, useState } from "react";
import { toast } from "sonner";

export function CultivationProjectDetail({ data }: { data: any }) {
  const readings = data.readings ?? []; const devices = data.devices ?? []; const zones = data.zones ?? [];
  const stages = [{ title: "Semilla y germinación", progress: 38, detail: "Humedad, luz y ventilación observadas." }, { title: "Desarrollo vegetativo", progress: 62, detail: "Nutrientes y crecimiento modelados." }, { title: "Cosecha y trazabilidad", progress: 18, detail: "Criterios de calidad preparados." }];
  return <div className="space-y-6"><section className="rounded-3xl border border-emerald-300/15 bg-[#0b1712]/80 p-6"><div className="grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><div><div className="flex items-center gap-2 text-emerald-100"><Sprout className="size-4" /><p className="text-[10px] font-semibold uppercase tracking-[0.2em]">Agrotech · gemelo digital</p></div><h2 className="mt-3 font-display text-3xl text-slate-100">Cultivo autónomo y resiliente</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-400">Una vista de ciclo completo para planificar cultivo, recursos, telemetría y salvaguardas. Las recomendaciones permanecen en observación o simulación; ningún riego, dosificación o corte se envía al mundo físico.</p><div className="mt-5 flex flex-wrap gap-2"><Badge variant="outline" className="border-emerald-300/20 text-emerald-100"><Leaf className="mr-1.5 size-3.5" />{zones.length} zonas modeladas</Badge><Badge variant="outline" className="border-cyan-300/20 text-cyan-100"><Droplets className="mr-1.5 size-3.5" />{readings.length} lecturas de referencia</Badge><Badge variant="outline" className="border-amber-300/20 text-amber-100"><ShieldCheck className="mr-1.5 size-3.5" />Control físico aislado</Badge></div></div><div className="rounded-2xl border border-emerald-300/10 bg-slate-950/30 p-5"><p className="text-[10px] uppercase tracking-[0.16em] text-slate-500">Recursos de referencia</p><div className="mt-4 grid grid-cols-2 gap-3"><Metric icon={Waves} label="Agua" value="Modelada" /><Metric icon={Sparkles} label="Energía" value={`${data.lab?.energyReservePct ?? "—"}%`} /><Metric icon={Droplets} label="Nutrientes" value="Observación" /><Metric icon={Bot} label="Actuación" value="Bloqueada" /></div></div></div></section><section className="grid gap-4 xl:grid-cols-[1.05fr_.95fr]"><div className="rounded-2xl border border-white/5 bg-[#0a121d]/80 p-5"><p className="text-[10px] font-semibold uppercase tracking-[0.2em] text-emerald-200">Ciclo de cultivo</p><div className="mt-5 space-y-5">{stages.map((stage) => <div key={stage.title}><div className="flex items-center justify-between gap-3"><p className="text-sm text-slate-200">{stage.title}</p><span className="text-xs text-emerald-100">{stage.progress}%</span></div><Progress className="mt-2 h-1.5" value={stage.progress} /><p className="mt-2 text-xs text-slate-500">{stage.detail}</p></div>)}</div></div><div className="rounded-2xl border border-white/5 bg-[#0a121d]/80 p-5"><p className="text-[10px] font-semibold uppercase tracking-[0.2em] text-cyan-300">Zonas y límites</p><div className="mt-4 space-y-3">{zones.map((zone: any) => <div key={zone.id} className="rounded-xl border border-white/5 bg-slate-950/30 p-3"><div className="flex items-center justify-between gap-3"><p className="text-sm text-slate-200">{zone.name}</p><Badge variant="outline" className="border-white/10 text-[10px] text-slate-400">{zone.status}</Badge></div><p className="mt-1 text-xs text-slate-500">{zone.description}</p></div>)}<p className="rounded-xl border border-amber-300/15 bg-amber-300/[0.03] p-3 text-xs leading-5 text-amber-100">Todo plan de riego, nutrientes o cosecha exige umbrales, simulación, evidencia y aprobación humana.</p></div></div></section></div>;
}

function Metric({ icon: Icon, label, value }: any) { return <div className="rounded-xl border border-white/5 bg-white/[0.02] p-3"><Icon className="size-4 text-emerald-100" /><p className="mt-2 text-[10px] uppercase tracking-[0.12em] text-slate-600">{label}</p><p className="mt-1 text-sm text-slate-200">{value}</p></div>; }

export function AssistiveTechnologyDetail() {
  const modules = [{ icon: Accessibility, title: "Movilidad guiada", detail: "Rutas, postura y llamadas de ayuda simuladas; sin autonomía física.", state: "Diseño" }, { icon: BedDouble, title: "Cama y transferencias", detail: "Rutinas de confort y asistencia digital; revisión clínica obligatoria.", state: "Investigación" }, { icon: Utensils, title: "Apoyo a alimentación", detail: "Planificación de utensilios, ritmos y preferencias; sin dispensado físico.", state: "Diseño" }, { icon: HeartHandshake, title: "Cuidado y comunicación", detail: "Recordatorios, consentimiento y coordinación con cuidadores.", state: "Prototipo digital" }];
  return <div className="space-y-6"><section className="rounded-3xl border border-rose-300/15 bg-[#171015]/75 p-6"><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="text-[10px] font-semibold uppercase tracking-[0.2em] text-rose-200">Tecnología asistiva · diseño centrado en la persona</p><h2 className="mt-2 font-display text-3xl text-slate-100">Apoyos para autonomía cotidiana</h2><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-400">Esta interfaz organiza investigación y escenarios de asistencia para movilidad, cama, alimentación y comunicación. No es un dispositivo médico ni ejecuta acciones físicas: requiere validación clínica, consentimiento y evaluación de riesgos antes de cualquier prototipo.</p></div><Accessibility className="size-8 text-rose-200" /></div><div className="mt-6 grid gap-3 md:grid-cols-2">{modules.map((module) => <div key={module.title} className="rounded-2xl border border-white/5 bg-slate-950/30 p-4"><module.icon className="size-5 text-rose-200" /><div className="mt-3 flex items-center justify-between gap-3"><h3 className="text-base text-slate-100">{module.title}</h3><Badge variant="outline" className="border-rose-300/20 text-[10px] text-rose-100">{module.state}</Badge></div><p className="mt-2 text-xs leading-5 text-slate-500">{module.detail}</p></div>)}</div></section><section className="rounded-2xl border border-amber-300/15 bg-amber-300/[0.025] p-5"><p className="flex items-center gap-2 text-sm text-amber-100"><ShieldCheck className="size-4" />Pasarela de seguridad para cualquier avance</p><div className="mt-4 grid gap-3 md:grid-cols-4">{["Necesidad definida", "Revisión clínica", "Consentimiento informado", "Simulación y evidencia"].map((step, index) => <div key={step} className="flex items-center gap-2 text-xs text-slate-400"><span className="grid size-6 place-items-center rounded-full border border-amber-300/20 text-amber-100">{index + 1}</span>{step}</div>)}</div></section></div>;
}

export function CleaningAutomatonDetail() {
  const [area, setArea] = useState("Cocina");
  const tasks: Record<string, { title: string; detail: string; risk: string }[]> = { Cocina: [{ title: "Lavar vajilla", detail: "Clasifica material y estima agua; sin activar lavavajillas o químicos.", risk: "medio" }, { title: "Separar reciclaje", detail: "Identifica vidrio, metal, plástico y orgánicos para revisión humana.", risk: "bajo" }], Sala: [{ title: "Limpieza de superficies", detail: "Genera una ruta y lista de materiales sin operar aspiradoras o pulidoras.", risk: "bajo" }, { title: "Encerado simulado", detail: "Evalúa superficie, ventilación y riesgos de resbalón; no aplica productos.", risk: "medio" }], Exterior: [{ title: "Lavado de vehículo", detail: "Modela consumo de agua y materiales; no activa mangueras ni equipo.", risk: "medio" }, { title: "Gestión de residuos", detail: "Planifica recolección, reciclaje y punto de salida seguro.", risk: "bajo" }] };
  return <div className="space-y-6"><section className="rounded-3xl border border-sky-300/15 bg-[#09131d]/80 p-6"><div className="grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><div><div className="flex items-center gap-2 text-sky-100"><Bot className="size-4" /><p className="text-[10px] font-semibold uppercase tracking-[0.2em]">Nueva iniciativa · limpieza doméstica</p></div><h2 className="mt-3 font-display text-3xl text-slate-100">Autómata de limpieza y reciclaje</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-400">Un gemelo digital para planificar limpieza de cocina, sala, exteriores, lavado de vajilla, reciclaje y residuos. Solo crea planes supervisados: locomoción, químicos, agua y manipulación de objetos permanecen bloqueados.</p><div className="mt-5 flex flex-wrap gap-2"><Badge variant="outline" className="border-sky-300/20 text-sky-100"><Recycle className="mr-1.5 size-3.5" />Reciclaje trazable</Badge><Badge variant="outline" className="border-cyan-300/20 text-cyan-100"><Trash2 className="mr-1.5 size-3.5" />Residuos clasificados</Badge><Badge variant="outline" className="border-amber-300/20 text-amber-100"><ShieldCheck className="mr-1.5 size-3.5" />Sin actuación física</Badge></div></div><div className="rounded-2xl border border-white/5 bg-slate-950/30 p-5"><p className="text-[10px] uppercase tracking-[0.16em] text-slate-600">Flujo de residuos</p><div className="mt-5 space-y-3">{["Identificar material", "Clasificar y contener", "Validar seguridad", "Entregar a revisión"].map((item, index) => <div key={item} className="flex items-center gap-3"><span className="grid size-7 place-items-center rounded-full border border-sky-300/20 text-xs text-sky-100">{index + 1}</span><p className="text-sm text-slate-300">{item}</p></div>)}</div></div></div></section><section className="rounded-2xl border border-white/5 bg-[#0a121d]/80 p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[0.2em] text-sky-300">Planificador de tareas</p><h3 className="mt-1 font-display text-xl text-slate-100">Zona de limpieza simulada</h3></div><div className="flex gap-2">{Object.keys(tasks).map((item) => <Button key={item} variant="outline" onClick={() => setArea(item)} className={area === item ? "border-sky-300/35 bg-sky-300/[0.08] text-sky-100" : "border-white/10 text-slate-400"}>{item}</Button>)}</div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{tasks[area].map((task) => <div key={task.title} className="rounded-xl border border-white/5 bg-slate-950/25 p-4"><div className="flex items-start justify-between gap-3"><h4 className="text-sm text-slate-200">{task.title}</h4><Badge variant="outline" className="border-amber-300/20 text-[10px] text-amber-100">riesgo {task.risk}</Badge></div><p className="mt-2 text-xs leading-5 text-slate-500">{task.detail}</p><p className="mt-3 flex items-center gap-1.5 text-[11px] text-sky-100"><CircleCheck className="size-3.5" />Preparada para simulación y aprobación humana</p></div>)}</div></section></div>;
}

export function VoiceRecognitionControl() {
  const [status, setStatus] = useState<"listo" | "solicitando" | "escuchando" | "resultado" | "error">("listo"); const [text, setText] = useState(""); const recognitionRef = useRef<any>(null);
  const start = () => { const Recognition = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition; if (!Recognition) { setStatus("error"); toast.error("El reconocimiento local no está disponible; usa la transcripción segura por servicio."); return; } const recognition = new Recognition(); recognition.lang = "es-ES"; recognition.interimResults = true; recognition.continuous = false; recognition.onstart = () => setStatus("escuchando"); recognition.onresult = (event: any) => { const value = Array.from(event.results).map((result: any) => result[0]?.transcript ?? "").join(" "); setText(value); }; recognition.onerror = () => { setStatus("error"); toast.error("Revisa el permiso de micrófono o utiliza la transcripción por servicio."); }; recognition.onend = () => setStatus(text ? "resultado" : "listo"); recognitionRef.current = recognition; setStatus("solicitando"); recognition.start(); };
  return <section className="rounded-2xl border border-cyan-300/15 bg-cyan-300/[0.025] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[0.2em] text-cyan-300">Reconocimiento de voz · estado visible</p><h3 className="mt-1 font-display text-lg text-slate-100">Chequeo de micrófono y transcripción local</h3><p className="mt-2 text-xs leading-5 text-slate-500">Este control explica permisos, captura una transcripción revisable y ofrece el servicio seguro como alternativa. No guarda ni ejecuta ninguna acción por sí solo.</p></div><BrainCircuit className="size-5 text-cyan-200" /></div><div className="mt-4 flex flex-wrap items-center gap-3"><Badge variant="outline" className="border-cyan-300/20 text-cyan-100"><Ear className="mr-1.5 size-3.5" />Estado: {status}</Badge><Button onClick={status === "escuchando" ? () => recognitionRef.current?.stop() : start} className={status === "escuchando" ? "bg-rose-300 text-slate-950 hover:bg-rose-200" : "bg-cyan-300 text-slate-950 hover:bg-cyan-200"}>{status === "escuchando" ? <MicOff className="mr-2 size-4" /> : <Mic className="mr-2 size-4" />}{status === "escuchando" ? "Detener escucha" : "Probar micrófono"}</Button></div>{text ? <p className="mt-4 rounded-xl border border-white/5 bg-slate-950/30 p-3 text-sm text-slate-300">{text}</p> : null}</section>;
}
